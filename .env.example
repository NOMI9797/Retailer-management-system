# Postgres connection string (Neon or Supabase)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Temporary — used by getCurrentShopId() until Clerk auth is wired in.
# Set this to a shop's id after your first `prisma migrate dev` + seed.
DEV_SHOP_ID=""

# Added when auth is wired in (build order step 1 / step 9)
# CLERK_SECRET_KEY=""
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
